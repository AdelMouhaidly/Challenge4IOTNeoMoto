services:
  - type: web
    name: mottu-python-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn detect_motos:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000

